package inf112.skeleton.app.card;

import inf112.skeleton.app.shared.Action;

public class RotateRight extends Card {
    public RotateRight(int priority, Action action) {
        super(priority, action);
    }
}
